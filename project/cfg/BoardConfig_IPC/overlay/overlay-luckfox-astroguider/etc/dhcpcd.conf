# A sample configuration for dhcpcd.
# See dhcpcd.conf(5) for details.

# Allow users of this group to interact with dhcpcd via the control socket.
#controlgroup wheel

# Inform the DHCP server of our hostname for DDNS.
#hostname

# Use the hardware address of the interface for the Client ID.
#clientid
# or
# Use the same DUID + IAID as set in DHCPv6 for DHCPv4 ClientID as per RFC4361.
# Some non-RFC compliant DHCP servers do not reply with this set.
# In this case, comment out duid and enable clientid above.
duid

# Persist interface configuration when dhcpcd exits.
persistent

# vendorclassid is set to blank to avoid sending the default of
# dhcpcd-<version>:<os>:<machine>:<platform>
vendorclassid

# A list of options to request from the DHCP server.
option domain_name_servers, domain_name, domain_search
option classless_static_routes
# Respect the network MTU. This is applied to DHCP routes.
option interface_mtu

# Request a hostname from the network
option host_name

# Most distributions have NTP support.
#option ntp_servers

# Rapid commit support.
# Safe to enable by default because it requires the equivalent option set
# on the server to actually work.
option rapid_commit

# A ServerID is required by RFC2131.
require dhcp_server_identifier

# Generate SLAAC address using the Hardware Address of the interface
#slaac hwaddr
# OR generate Stable Private IPv6 Addresses based from the DUID
slaac private

# OpenAstroGuider (2026-09-17): usb0 is a point-to-point USB RNDIS gadget
# link -- there is never a real DHCP server on the other end, so every second
# dhcpcd spends soliciting one, and then probing for an IPv4LL address, is
# pure added latency before an ASCOM client can see the camera. Measured: 5s
# of DHCP plus 5s of RFC 3927 probing, ~10s of the ~16s to a usable address.
#
# S51usb0-linklocal now assigns usb0's address directly, derived from the
# pinned gadget MAC, so dhcpcd must stay off this interface entirely or the
# two will fight (the old static-vs-dhcpcd race is exactly how the original
# 172.32.0.70 problem manifested). eth0 is unaffected and still uses dhcpcd
# normally.
#
# NOTE: an `interface usb0 { timeout 1 }` block used to live here, intended to
# shorten the DHCP half. It was measured on hardware (2026-09-17) and has NO
# effect -- dhcpcd decides to fall back on its DISCOVER retransmit schedule,
# not on `timeout`, so the phase took 5s with and without it. Removed rather
# than left in place looking like it does something.
denyinterfaces usb0
